import React from 'react';
import Nav from '../Nav/Nav';
import { Container } from './index';

const Header = () => {
    return (
        <Container>
            <Nav />
        </Container>
    )
};

export default Header;
