import styled from 'styled-components';

export const Main = styled.main`
  max-width: 1400px;
  margin: 0 auto 2rem;
  padding: 0 2rem;
  text-align: center;
`;

export const Wrapper = styled.div`
    position: relative;
    min-height: 100%;
`;
